# Changes made:

* In `docker-compose.yml`, overrode the environment variables `API_URL` and `FRONT_URL` set in both Dockerfiles.

Frontend Dockerfile can be found in the part1/1.10 folder.
Backend Dockerfile can be found in the part1/1.11 folder